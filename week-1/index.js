const mathFunctions = require('./funcitons');

// Window refers to the browser window; Node.js doesn't have a browser, so it doesn't work. 
// console.log(window);

console.log("Help!!!");

console.log(mathFunctions.quadraticFormula(1, 2, 1));